import ExampleComponent from './components/ExampleComponent';

export default {
    mode: 'history',

    routes: [
        {
            path: '/home',
            component: ExampleComponent
        },
    ]
}
